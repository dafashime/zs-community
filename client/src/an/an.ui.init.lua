an.btn = import(".btn")
an.input = import(".input")
an.label = import(".label")
an.labelM = import(".labelM")
an.loading = import(".loading")
an.msgbox = import(".msgbox")
an.progress = import(".progress")
an.scroll = import(".scroll")
an.slider = import(".slider")
an.toggle = import(".toggle")
an.voiceBubble = import(".voiceBubble")
an.netSprite = import(".netSprite")
an.z = import(".z")

function an.newBtn(...)
	return an.btn.new(...)
end

function an.newInput(...)
	return an.input.new(...)
end

function an.newLabel(...)
	return an.label.new(...)
end

function an.newLabelM(...)
	return an.labelM.new(...)
end

function an.newLoading(...)
	return an.loading.new(...)
end

function an.newMsgbox(...)
	return an.msgbox.new(...)
end

function an.newProgress(...)
	return an.progress.new(...)
end

function an.newScroll(...)
	return an.scroll.new(...)
end

function an.newSlider(...)
	return an.slider.new(...)
end

function an.newToggle(...)
	return an.toggle.new(...)
end

function an.newVoiceBubble(...)
	return an.voiceBubble.new(...)
end

function an.newNetSprite(...)
	return an.netSprite.new(...)
end
